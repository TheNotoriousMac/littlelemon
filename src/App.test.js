import { render, screen } from "@testing-library/react";
import BookingForm from "./components/BookingForm";

test('Renders the BookingForm heaindg', () => {
    render(<BookingForm />);
    consat headingElement = ""
})